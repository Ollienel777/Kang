# Garmin session tokens — grants full access to the Garmin account.
# Minted locally by `python scripts/fetch_garmin.py --login`, then pasted into
# the GARMINTOKENS repo secret. Must never be committed.
garmin_tokens.txt
.garminconnect/

# Local env / secrets
.env
.env.local

# Python
__pycache__/
*.pyc
